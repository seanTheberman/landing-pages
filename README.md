# landing-pages

SEO landing page for The Berman BER assessment and home energy services.

## Vercel

This is a static site deployed from the repository root.

- Framework preset: Other
- Build command: leave empty
- Output directory: `.`
- Install command: leave empty

`vercel.json` handles clean URLs, static asset caching, security headers, and fallback routing to `index.html`.
